# ============================================================
# Pictory Script Evaluation Pipeline - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env-example .env
# ============================================================

AWS_PROFILE=Prod-read-701488979254
AWS_DEFAULT_REGION=us-east-2



# ------------------------------------------------------------
# AWS Configuration (for Bedrock + S3)
# ------------------------------------------------------------
AWS_PROFILE=Interns-aws-access-284231530171
AWS_DEFAULT_REGION=us-east-2

# ------------------------------------------------------------
# OpenAI Configuration (for script generation + alternate judge)
# ------------------------------------------------------------
OPEN_API=sk-...

# ------------------------------------------------------------
# Judge Model Configuration
# ------------------------------------------------------------
# Provider: "bedrock" or "openai"
JUDGE_PROVIDER=bedrock

# Bedrock judge model (used when JUDGE_PROVIDER=bedrock)
JUDGE_MODEL_BEDROCK=moonshot.kimi-k2-thinking

# OpenAI judge model (used when JUDGE_PROVIDER=openai)
JUDGE_MODEL_OPENAI=gpt-4o

# ------------------------------------------------------------
# Model Summary
# ------------------------------------------------------------
# Role                    | Model              | API
# ------------------------|--------------------|-----------------
# Old script generation   | gpt-4o-mini        | OpenAI (OPEN_API)
# New script generation   | gpt-4.1-mini       | OpenAI (OPEN_API)
# Template classification | gpt-4o-mini        | OpenAI (OPEN_API)
# Judge (primary)         | Kimi K2 Thinking   | Bedrock (AWS_PROFILE)
# Judge (alternate)       | gpt-4o / gpt-5.2   | OpenAI (OPEN_API)
# ------------------------------------------------------------
